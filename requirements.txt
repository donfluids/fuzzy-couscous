# Python dependencies for the fuzzy-couscous post-processing (postprocessing/).
# The solver itself is C++/CMake and needs none of these (see README "Requirements").
#
#   python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
#
# Lower bounds are the versions the analysis suite is tested against (Python 3.12).
numpy>=1.26
scipy>=1.11
h5py>=3.10
pandas>=2.1
matplotlib>=3.6
